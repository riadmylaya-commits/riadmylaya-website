name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: |
          curl -sf https://riadmylaya-website.onrender.com > /dev/null || true
          echo "Pinged at $(date -u)"
